Python
s = set()
s.add(1)
s.add(3)
s.add(3)
s.add(5)
print(s)
Items in the set are unique, gives {1,3 5}

Dictionaries
ages = {}"Alice": 22, "Bob": 27}
ages["Charlie"] = 30
ages["Alice"] += 1

for i in range(10):
  print("{} squared is {}".format(i, square(i)))
  // will plug in i, square(i) in the curlies

  if __name__ == "__main__": // If I am currently running this file, execute main
    main()

  class Point:
    def __Init__(self, x, y): // when I create a new point, what information do I need?
      self.x = x
      self.y = y

  Capitalize each letter
    name = name.capitalize()

new_year = now.month == 1 and now.day == 1 // boolean value new_year, true if both conditions apply

  Flask
  normally stored as application.py

  app = Flask(__name__) // want to create a new application that is a Flask application (__name__ is saying "this file")
  @app.route("/") // when user goes to the route "/" the function immediately underneath is run

  to run flask, "flask run"

  run flask for the first time:
  export FLASK_APP=application.py

  Jinja2 Language in HTML
  {{ headline }} // placeholder for a variable from outside
  {% if new_year %} // introduces condition
  {{ url_for('more')}} // function url_for in Jinja is saying the link for "more"
