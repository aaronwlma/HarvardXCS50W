CS50W: Web Programming with Python and JavaScript

Lecture 0
Git Commands:
  git clone <url> // Copies GitHub Internet repository to local storage
  git add <filename> // Tells git to follow local file (for committing later)
  git commit -m "message" // Tells git to commit the added files to the Internet repository, -m tags the message in quotes
    -a // Adds file to staging area, so you can simplify by using -am
  git status // Tells us what's currently going on in our repository
  git push // Pushes the files to the Internet repository that was labeled for committing
  git pull // Pulls the latest version of the files in the Internet repository to local storage
  git log // Shows history of the commits we have made
  git reset --hard <commit> // Resets your repository to a certain commit hash
  get reset --hard origin/master // Resets your repository to whatever you got the code from initially
  get reflog // Displays reference logs
HTML:
  
